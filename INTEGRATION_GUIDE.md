# Руководство по интеграции Frontend + AI Chat Bot

Этот документ описывает, как запустить полную систему с интеграцией фронтенда и AI чат-бота.

## Архитектура системы

```
┌─────────────────┐         ┌──────────────────┐         ┌─────────────────┐
│   Frontend      │────────▶│   Main API       │────────▶│   Database      │
│   (Port 8080)   │         │   (Port 8000)    │         │   (SQLite)      │
└────────┬────────┘         └──────────────────┘         └─────────────────┘
         │
         │
         ▼
┌─────────────────┐
│   AI Chat API   │
│   (Port 8001)   │
└─────────────────┘
```

## Компоненты

### 1. Frontend (Port 8080)
- Интерфейс платформы вакансий
- AI чат-виджет
- Технологии: HTML, CSS, Vanilla JavaScript

### 2. Main API (Port 8000)
- Основной API для вакансий и компаний
- Технологии: FastAPI, SQLAlchemy, SQLite

### 3. AI Chat API (Port 8001)
- AI помощник для подбора вакансий
- Технологии: FastAPI, OpenAI API

## Запуск системы

### Шаг 1: Установка зависимостей

```bash
# Основной API
pip install fastapi uvicorn sqlalchemy pydantic

# AI Chat API
cd ai-engineer
pip install -r requirements.txt
cd ..
```

### Шаг 2: Настройка OpenAI API ключа

```bash
cd ai-engineer
cp .env.example .env
# Отредактируйте .env и добавьте ваш OpenAI API ключ
nano .env  # или используйте любой текстовый редактор
```

Получить API ключ можно на [platform.openai.com/api-keys](https://platform.openai.com/api-keys)

### Шаг 3: Инициализация базы данных

```bash
# Создать базу данных с примерами вакансий
python init_db.py
```

### Шаг 4: Запуск серверов

Откройте **3 терминала** и запустите каждый сервис:

#### Терминал 1 - Main API (Port 8000)
```bash
python -m uvicorn main:app --reload --host 127.0.0.1 --port 8000
```

#### Терминал 2 - AI Chat API (Port 8001)
```bash
cd ai-engineer
python -m uvicorn main:app --reload --host 127.0.0.1 --port 8001
```

#### Терминал 3 - Frontend (Port 8080)
```bash
cd frontend
python -m http.server 8080
```

### Шаг 5: Откройте в браузере

```
http://localhost:8080
```

## Использование AI чат-бота

1. **Откройте чат**: Нажмите на синюю кнопку с иконкой чата в правом нижнем углу
2. **Задайте вопрос**: Например:
   - "Какие есть вакансии для Python разработчика?"
   - "Посоветуй вакансию для middle разработчика"
   - "Какие навыки нужны для Data Scientist?"
3. **Получите рекомендации**: AI проанализирует вакансии и даст персонализированные советы

## Примеры вопросов для AI

- 🔍 **Поиск вакансий**:
  - "Найди вакансии в Алматы"
  - "Покажи remote вакансии для senior разработчиков"

- 💼 **Анализ требований**:
  - "Какие требования у вакансии Data Scientist?"
  - "Что нужно знать для работы в Google?"

- 📚 **Развитие навыков**:
  - "Какие навыки мне нужно подтянуть для перехода на senior?"
  - "Посоветуй технологии для изучения"

- 🎯 **Персональные рекомендации**:
  - "Я знаю Python и Django, какие вакансии мне подойдут?"
  - "У меня 3 года опыта в backend, что порекомендуешь?"

## Проверка работы системы

### Main API (Port 8000)
```bash
# Проверить список вакансий
curl http://localhost:8000/api/v1/jobs

# Открыть API документацию
open http://localhost:8000/docs
```

### AI Chat API (Port 8001)
```bash
# Проверить статус
curl http://localhost:8001/api/health

# Тестовый запрос к чат-боту
curl -X POST http://localhost:8001/api/chat \
  -H "Content-Type: application/json" \
  -d '{
    "message": "Привет, покажи вакансии для Python разработчика",
    "conversation_history": [],
    "user_skills": ["Python"],
    "user_experience": "middle"
  }'

# Открыть API документацию
open http://localhost:8001/docs
```

### Frontend (Port 8080)
```bash
# Проверить доступность
curl -I http://localhost:8080
```

## Troubleshooting

### Проблема: AI чат не отвечает

**Решение:**
1. Проверьте, что AI сервер запущен на порту 8001:
   ```bash
   curl http://localhost:8001/api/health
   ```
2. Проверьте консоль браузера (F12) на наличие ошибок
3. Убедитесь, что OPENAI_API_KEY установлен в `.env` файле

### Проблема: Вакансии не загружаются

**Решение:**
1. Проверьте, что Main API запущен на порту 8000
2. Проверьте, что база данных инициализирована:
   ```bash
   ls -la job_platform.db
   ```
3. Инициализируйте БД если нужно:
   ```bash
   python init_db.py
   ```

### Проблема: CORS ошибки

**Решение:**
Убедитесь, что оба API сервера имеют CORS middleware настроенный:
```python
app.add_middleware(
    CORSMiddleware,
    allow_origins=["*"],
    allow_credentials=True,
    allow_methods=["*"],
    allow_headers=["*"],
)
```

### Проблема: Порты заняты

**Решение:**
Измените порты в конфигурации:
- Frontend: `chat.js` → `AI_API_BASE_URL`
- Main API: команда запуска → `--port XXXX`
- AI API: команда запуска → `--port XXXX`

## Файловая структура

```
hh-ai-nfactorial/
├── frontend/                   # Frontend приложение
│   ├── index.html             # Главная страница
│   ├── styles.css             # Стили + стили чата
│   ├── app.js                 # Основной JS
│   ├── chat.js                # AI чат функциональность
│   ├── DEMO.md
│   ├── OVERVIEW.md
│   └── README.md
├── ai-engineer/               # AI Chat Bot
│   ├── main.py               # FastAPI сервер
│   ├── chat_service.py       # OpenAI интеграция
│   ├── models.py             # Pydantic модели
│   ├── database.py           # Мок данные
│   ├── requirements.txt
│   ├── .env                  # OpenAI API ключ
│   └── README.md
├── main.py                    # Main API server
├── database.py                # Database setup
├── models.py                  # SQLAlchemy models
├── schemas.py                 # Pydantic schemas
├── crud.py                    # Database operations
├── init_db.py                 # Database initialization
├── job_platform.db            # SQLite database
└── INTEGRATION_GUIDE.md       # Этот файл
```

## API Endpoints

### Main API (Port 8000)

| Method | Endpoint | Description |
|--------|----------|-------------|
| GET | `/api/v1/jobs` | Получить все вакансии |
| GET | `/api/v1/jobs/{id}` | Получить вакансию по ID |
| GET | `/api/v1/companies` | Получить все компании |
| GET | `/docs` | API документация |

### AI Chat API (Port 8001)

| Method | Endpoint | Description |
|--------|----------|-------------|
| POST | `/api/chat` | Отправить сообщение чат-боту |
| POST | `/api/recommendations` | Получить рекомендации |
| GET | `/api/vacancies` | Получить вакансии (мок) |
| GET | `/api/health` | Проверка здоровья сервиса |
| GET | `/docs` | API документация |

## Дальнейшее развитие

### Возможные улучшения:

1. **Аутентификация пользователей**
   - JWT токены
   - Сохранение профиля пользователя
   - История чата для каждого пользователя

2. **Продвинутые возможности чата**
   - Голосовой ввод
   - Отправка файлов (резюме)
   - Быстрые ответы (Quick replies)

3. **Интеграция с основным API**
   - AI использует реальные данные из основной БД
   - Унификация моделей данных
   - Единая база данных

4. **Аналитика**
   - Трекинг вопросов пользователей
   - Популярные запросы
   - Успешность рекомендаций

5. **UI/UX улучшения**
   - Анимации
   - Звуковые уведомления
   - Темная тема
   - Мультиязычность

## Лицензия

MIT

## Поддержка

При возникновении проблем:
1. Проверьте логи серверов в терминалах
2. Откройте консоль браузера (F12)
3. Проверьте документацию каждого компонента
